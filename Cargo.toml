[package]
name = "oci-interceptor"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.44"
clap = "2.33.3"
oci-spec = { git = "https://github.com/containers/oci-spec-rs", branch = "main"}  # Relying on setters introduced in #76 but not released yet

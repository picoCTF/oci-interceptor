[package]
authors = ["picoCTF team <opensource@picoctf.org>"]
description = "OCI runtime wrapper for modifying container configs before creation"
edition = "2021"
license = "MIT"
name = "oci-interceptor"
publish = false
repository = "https://github.com/picoCTF/oci-interceptor"
version = "0.2.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.69"
clap = { version = "4.1.8", features = ["cargo"] }
oci-spec = "0.6.0"
serde_json = "1.0.93"
